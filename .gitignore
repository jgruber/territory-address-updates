# Data directories — never commit source files or outputs
data/
# Runtime credentials override (default credentials.json in repo root is tracked)
data/credentials.json

# Runtime files (credentials.json is tracked as the default; ignore local overrides via volume)
users.json
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
env/

# Dist / build
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
